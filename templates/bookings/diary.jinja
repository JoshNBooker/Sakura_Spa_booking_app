{% extends 'base.jinja' %}{% block content %}
    <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title }}</title>
    </head>
    <body>
        </a>
        <form action="/bookings/<id>">
            <label for="customer">Which customer would you like to access?</label>
            <select name="customer" id="customer">
                {% for customer in customers %}
                    <option 
                    value="{{ customer.id }}">
                    {{ customer.first_name }} {{ customer.last_name }}
                    </option>
                {% endfor %}
            </select>
            <a href="/bookings/<id>">
                <submit><button>Show bookings</button></submit>
            </a>
        </form>
    </body>
    </html>


{% endblock content %}